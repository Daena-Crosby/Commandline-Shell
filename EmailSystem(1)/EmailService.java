package EmailSystem;

import java.util.ArrayList;
import java.util.List;
import EmailSystem.Email;

public class EmailService {

    // Constructor
    public EmailService() {

    }

    // Send an email
    public String sendEmail(Email email, User sender) {

    }

    // Fetch the inbox for a specific user
    public List<Email> fetchInbox(User user) {

    }

    // Fetch sent emails for a specific user
    public List<Email> fetchSent(User user) {

    }

    // Delete an email by ID
    public void deleteEmail(int emailId) {
    }

    // Fetch details of a specific email
    public Email fetchEmailDetails(int emailId) {

    }

    // Optional: Add a user (register simulation)
    public void addUser(User user) {
    }

    // Optional: Get all users
    public List<User> getUsers() {
    }
}

The UWI
Mona Computing
Society is
a student
organization based
in the
Department of
Computing at
the University
of the
West Indies
Mona campus.
We are
the UWI
Mona Computing
Society and
we aim
to aid
in the
development of
students competencies
within the
field of Computing
.